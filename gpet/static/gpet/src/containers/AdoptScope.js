import React from 'react'
import AdoptMain from '../components/adoptComponent/AdoptMain'

const AdoptScope = ({ history }) => {
    return <AdoptMain history={history} />
}

export default AdoptScope
